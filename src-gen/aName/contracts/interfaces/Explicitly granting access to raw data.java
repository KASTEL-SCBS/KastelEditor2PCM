package aName.contracts.interfaces;

import aName.contracts.datatypes.Data;
		
public interface Explicitly granting access to raw data {
			
	void explicitly granting access to raw data(Data data); 

}